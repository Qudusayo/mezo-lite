{
  "name": "mezo-lite",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "contracts"
  ],
  "devDependencies": {},
  "scripts": {
    "ios": "pnpm -r run ios",
    "prebuild": "pnpm -r run prebuild",
    "prebuild-clean": "pnpm -r run prebuild-clean",
    "dev": "pnpm -r run dev",
    "build": "pnpm -r run build",
    "android": "pnpm -r run android"
  },
  "dependencies": {
    "expo": "~53.0.22",
    "react": "19.0.0",
    "react-native": "0.79.6"
  }
}
